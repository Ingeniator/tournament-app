// Re-export from @padel/common — this file is kept for backward compatibility
export { resolveConfigDefaults, computeSitOutInfo, MINUTES_PER_POINT, MINUTES_PER_GAME, MINUTES_PER_SET, CHANGEOVER_MINUTES, DEFAULT_MINUTES_PER_ROUND } from '@padel/common';
export type { SitOutInfo } from '@padel/common';
